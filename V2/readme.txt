此版本的工作:
1:服务端应当可以同时处理多个客户端的连接，因此要使用线程处理每个客户端。
  与聊天室的解决方案一致，定义线程任务类ClientHandler，用线程来处理客
  户端的交互。
  ClientHandler中定义与客户端的交互过程，按照HTTP协议的交互要求(一问一
  答)，我们在  这里分三步完成交互，分别是:解析请求，处理请求，响应客户端。


2:由于HTTP协议中请求的前两部分有一些共同特点:都是文本数据，并且都是以
  回车+换行符作为结束的一行字符串。
  因此我们在解析请求前先测试读取一行字符串的逻辑。