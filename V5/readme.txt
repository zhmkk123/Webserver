解决空请求问题
之前解析请求时，通过测试有可能会在解析请求中的请求行工作中出现下标越界
的问题(读取请求行后按照空格拆分为三部分，并分别赋值给method,uri,protocol)。
这实际上是由于空请求的问题导致的。

HTTP协议上对此有说明:为了保证服务端的健壮性，应当忽略客户端可能发送的空
的请求。即:客户端连接服务端后，没有发送一个标准的HTTP请求。而是直接发送
一个CRLF。因此，当我们解析请求时读物读取请求行内容发现是空字符串
(readLine方法返回空字符串说明单独读取到了CRLF)则应当忽略本次处理，直接
与客户端断开连接即可。
